export class Article {
  content: string;
  coverImageUrl: string;
  description: string;
  subtitle: string;
  title: string;
  url: string;
  meta: string;
  id: number;   // no ID when fetch article, manually grant ID
}
